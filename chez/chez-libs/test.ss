
(def (fib0 n)
  ;(let ([st(clock)] [1st Fal])
    (def (_ n)
      (if (< n 3) 1 ;
        ; (bgn
          ; (if [=0 (%[- (clock) st]100)]
            ; (when 1st (setq 1st Fal) (echo ".") [k?(+ [_ (fx- n 1)] [_ (fx- n 2)])]) ;call/cc (lam(k)...) ;see to yin/yang
            ; (setq 1st Tru) )
          (+ [_ (fx- n 1)] [_ (fx- n 2)])
    ) ) ;)
    (if (< n 0) 0
      (_ n)
) ) ;)